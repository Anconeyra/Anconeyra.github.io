# Change: nextjs-vercel-migration

Proposed change directory initialized by `sdd-init` (2026-09-09).

Next steps:
1. `/sdd-propose nextjs-vercel-migration` — define intent, scope, and rollout/rollback plan.
2. `/sdd-spec nextjs-vercel-migration` — write delta specs per domain.
3. `/sdd-design nextjs-vercel-migration` — technical design (Next.js App Router, component map, Vercel config).
4. `/sdd-tasks nextjs-vercel-migration` — break into implementation tasks.
5. `/sdd-apply`, `/sdd-verify`, `/sdd-archive` — implement and close.

This folder will hold: `proposal.md`, `specs/`, `design.md`, `tasks.md`, `verify-report.md`, and `state.yaml`.
